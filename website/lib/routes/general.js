Router.route('/', {
  name: 'main',
  template: 'main'
});

Router.route('/contact', {
  name: 'contact',
  template: 'contact'
});

Router.route('/login', {
  name: 'login',
  template: 'login'
});

Router.route('/signup', {
  name: 'signup',
  template: 'signup'
});
